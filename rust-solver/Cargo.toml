[package]
name = "minesweeper-solver"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"
bit-set = "0.5"

[dev-dependencies]
proptest = "1.0"
rand = "0.8"